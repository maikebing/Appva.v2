@model Appva.Mcss.Web.ViewModels.PrepareEditSequenceViewModel

<div class="std-panel std-form lb-panel-small lb-panel">
	@using (Html.BeginForm())
 {
        <h3 class="section-title">@Localizer["Redigera_rad"]</h3>
        <div class="text medium">    
            @Html.LabelFor(m => m.Name)
            @Html.TextBoxFor(m => m.Name, new { @placeholder = Localizer["T__ex__dosett"].ToString() })
            @Html.ValidationMessageFor(m => m.Name)
        </div>
        @Html.AntiForgeryToken()
        @Html.Submit(Localizer["Spara"].ToString())
 }
</div>